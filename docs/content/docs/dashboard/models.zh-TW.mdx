---
title: 模型
description: 邏輯模型、上游重新導向、中繼資料與定價。
---

## 邏輯模型

客戶端請求的是邏輯模型名稱。Monoize 把這個名稱解析到你的 Provider 和 Channel。客戶端看到的名稱不需要與上游廠商的名稱一致。

一個邏輯模型可以對應到多個 Provider 下的多個 Channel。客戶端無需任何修改即可獲得容錯移轉和流量分配。

![模型資料庫](/images/en/models.webp)

## 重新導向

Channel 的模型條目可以用 `redirect` 指定真實的上游模型名稱。

範例：客戶端呼叫 `my-fast-model`。Channel A 將其重新導向到 `gpt-4o-mini`，Channel B 將其重新導向到 `claude-3-5-haiku-20241022`。計費和日誌始終顯示邏輯名稱。

## 模型定價

**模型**頁面是定價主控台，包含五個分頁：

- **模型定價**：每個模型一筆價格記錄。每筆記錄設定計費模式：`per_token`、`per_request` 或 `tiered_expr`。
- **未定價模型**：已對應但沒有啟用價格記錄的模型。
- **工具價格**：伺服端工具類別的價格。計量單位為 `1k_calls`、`minute` 和 `session`。
- **上游同步**：從 Models.dev、OpenRouter 或 new-api 執行個體匯入價格。
- **群組定價**：每個群組一個計費倍率。

Monoize 按以下順序計算每筆費用：

1. 根據模型價格記錄和實際用量計算基礎費用。
2. 乘以群組的 `billing_ratio`。
3. 乘以 Channel 模型條目的 `multiplier`。
4. 截斷為整數奈米美元。

Channel 的 `multiplier` 為 `0` 時，該 Channel 上的請求免費。

費用以奈米美元（nano-dollar）為單位累計到每個使用者的流水帳。

## 價格同步

1. 開啟**模型 → 上游同步**。
2. 選擇資料來源：Models.dev、OpenRouter 或 new-api 執行個體。
3. 點擊**預覽**。表格顯示新增記錄和變更欄位。
4. 點擊**套用**。Monoize 寫入變更並記錄一筆稽核記錄。

同步不會修改已鎖定的欄位。同步不會刪除手動建立的記錄。

## 模型列表

`GET /v1/models` 回傳呼叫方 API 金鑰可用的邏輯模型。結果反映金鑰的模型限制和群組歸屬。

## 模型萬用字元

變換規則和部分限制支援模型萬用字元：

- `*` 匹配任意長度的字元序列，包括空序列。
- `?` 恰好匹配一個字元。
- 匹配區分大小寫，並錨定整個模型名稱。

範例：`gpt-4*` 匹配 `gpt-4o` 和 `gpt-4o-mini`，不匹配 `o4-mini`。
